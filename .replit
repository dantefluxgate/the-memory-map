language = "nodejs"
run = "npm install && npm run build && NODE_ENV=production PORT=5000 node server.js"
entrypoint = "server.js"

[env]
NODE_ENV = "production"
PORT = "5000"

[nix]
channel = "stable-24_05"

[deployment]
run = ["sh", "-c", "npm install && npm run build && NODE_ENV=production PORT=5000 node server.js"]

[[ports]]
localPort = 5000
externalPort = 80
